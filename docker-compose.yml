version: '3.3'

services:
    heartbeat_time:
        build:
            context: .
            dockerfile: Dockerfile.heartbeat_time  
    actor:
        build:
            context: .
            dockerfile: Dockerfile.actor  
